package fluentcodes.sandbox;

import org.junit.Assert;
import org.junit.Test;

/**
 * Just to check if dependencies are ok
 */
public class StartTest {
    @Test
    public void check () {
        Assert.assertEquals(1, 1);
    }
}
